import React from 'react';

const SharedTrackDescription = ({ description }) => (
    <div className="shared-track-description">
        Description: {description}
    </div>
)

export default SharedTrackDescription;